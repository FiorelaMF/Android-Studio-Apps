<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.upc.ingenieroselectronicos.estudiantessotr">
    <!-- TODO 1: Utiliza los permisos de Android para la INternet, cámara y disco duro -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EstudiantesSOTR">
        <activity android:name=".MiPerfilActivity"></activity>
        <activity android:name=".cliente.MenuClienteActivity" />
        <activity android:name=".administrador.MenuAdminActivity" />
        <activity android:name=".RegistrarActivity" />
        <activity android:name=".OlvidoActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>